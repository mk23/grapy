grapy_global:
    hostname: localhost

    snmp_version: 2
    snmp_community: public

    include_dir: conf.d
    persist_dir: /tmp

    host_prefix: cluster01.myhost
    carbon_host: localhost
    carbon_port: 2004

    logger_dest: console:
